import CardShop from "../cardShop";
import"./Galerie.css";


function Galerie {
    title,
    paragraph,
    title2,
    paragraph2,
};

    return (
        <div className={"myClassName"}>
        <div className="Vintagew.">
        <h3>{title3}</h3>
        <h2>{title2}</h2>
        <p>{paragraph}</p>
        <p>{paragraph2}</p>
      </div>
      <div className="Modernw.">
      <CardShop
              id={artwork.title}
              src={className}
              paragraph={artwork.title}
              myClassName p={artwork.artist}
              <ButtonAdd />
            />
      </div>
 </div>
        
);

export default Galerie;